PORT=3000

# Supabase project settings
SUPABASE_URL=https://xlayeoozkhpitendyqxp.supabase.co
SUPABASE_ANON_KEY=sb_publishable_jjCCETj9FmXrluC5tmAXJA_hq-u2cnM

# Optional: restrict CORS in production (e.g. https://yourfrontend.com). Omit to allow all origins.
# CORS_ORIGIN=https://yourfrontend.com

# Required on the server (Render, etc.) so profiles/points/users APIs are not blocked by RLS.
# Supabase Dashboard → Settings → API → service_role (keep secret; never expose to browsers)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Currency units of order total per 1 loyalty point for POST /points/exchange-from-order (default: 10 → $10 = 1 point)
# LOYALTY_CURRENCY_UNITS_PER_POINT=10
